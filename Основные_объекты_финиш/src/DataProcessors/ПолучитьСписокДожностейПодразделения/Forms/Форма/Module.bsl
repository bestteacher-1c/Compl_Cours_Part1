
&НаКлиенте
Процедура ПолучитьСписок(Команда)

	Массив = НаСервереМожноВызватьСКлиента.СформироватьСписокДолжностей(Подразделение);
	
	Для Каждого Элемент Из Массив Цикл
		
		Сообщить(Элемент);
		
	КонецЦикла;

КонецПроцедуры
