
&НаКлиенте
Процедура ПолучитьСписок(Команда)

	Массив = НаСервереМожноВызватьСКлиента.ПолучитьСписокДолжностей(Подразделение);
	
	Для Каждого Элемент Из Массив Цикл
		
		Сообщить(Элемент);
		
	КонецЦикла;

КонецПроцедуры
